---
---

Bump the `@fission-ai/openspec` dev dependency from 1.5.0 to 1.6.0 and regenerate
the OpenSpec-managed Claude command and skill files with `openspec update`. OpenSpec
is a development-only tool for this repo's spec workflow — it is not part of the
published runtime — so there is no release impact.
