---
---

Docs only: lift four durable lessons out of `contributing/NOTES.md` into the
best-practices docs (the bundling-isolation invariant → coding §6.5, the
`noNonNullAssertion` test-file override → linting, real-browser hydration
validation → testing §11.4, the symlink-`node_modules` EBADF tell → worktree
guide), then compact NOTES.md by correcting two superseded "reverted approach"
passages and removing content now documented elsewhere. No code or release impact.
